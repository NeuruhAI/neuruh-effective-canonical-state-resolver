# Security

Fail closed on stale anchored revisions, forked lineage, broken chains, invalid or failed receipts, duplicate receipts, lifecycle mismatch, cross-stage revisions, ambiguous competing tips, missing evidence, wrong-target evidence, authority claims, unknown fields or tampering. Ambiguity is an explicit terminal outcome, never a guess.

Report issues via the repository issue tracker with the `security` label. Synthetic fixtures only; never file real production digests.
